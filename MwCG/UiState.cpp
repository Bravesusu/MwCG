#include "stdafx.h"
#include "UiState.h"

using namespace mw;

//template<class TView, class TDoc>
//UiState<TView, TDoc>::~UiState(void)
//{
//}

//template<class TView, class TDoc>
//mw::UiState<TView, TDoc>::UiState( const TView* pView, const TDoc* pDoc ) : pView_(pView), pDoc_(pDoc_)
//{
//	static_assert(std::is_base_of<CView, TView>::value, "TView must be CView based.");
//	static_assert(std::is_base_of<CDocument, TDoc>::value, "TView must be CView based.");
//}
//
//template<class TView, class TDoc>
//mw::UiState<TView, TDoc>::UiState()
//{
//	static_assert(std::is_base_of<CView, TView>::value, "TView must be CView based.");
//	static_assert(std::is_base_of<CDocument, TDoc>::value, "TView must be CView based.");
//}